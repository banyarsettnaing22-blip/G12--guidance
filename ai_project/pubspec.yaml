name: ai_project
description: "Grade 12 Academic Guidance Flutter Project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  appwrite: ^25.4.0
  # Supabase Flutter Package
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Asset ဖိုင်များ (logo1.png အပါအဝင် assets ဖိုဒါတစ်ခုလုံးကို အလိုအလျောက် ချိတ်ဆက်ပေးပါမည်)
  assets:
    - assets/
    # - assets/books/
    # - assets/questions/